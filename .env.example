# ── LLM Provider (choose one) ─────────────────────────────────────────────────

# Google Gemini (Recommended — best JSON quality)
GOOGLE_API_KEY=your_gemini_api_key_here

# Groq (Fast free tier)
GROQ_API_KEY=your_groq_api_key_here

# Required by CrewAI internally (set to NA if not using OpenAI)
OPENAI_API_KEY=NA
